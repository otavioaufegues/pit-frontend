import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 16,
    paddingTop: 16,
    backgroundColor: '#fff',
  },

  alignVertical: {
    alignItems: 'center',
  },
  
  reportAxis: {
    alignItems: 'center',
    paddingTop: 40,
  },
});
